sync:
  nodes:
    enabled: true
    syncAllNodes: true
    enableScheduler: true
  #persistentvolumeclaims:
    #enabled: true
  pods:
    enabled: true
    ephemeralContainers: false
    status: true

syncer:
  # Image to use for the syncer
  image: wuxs/vcluster:edge
